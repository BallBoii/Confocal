{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Confocal GUI",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/gui/confocal_gui.py",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            },
            "console": "integratedTerminal"
        }
    ]
}